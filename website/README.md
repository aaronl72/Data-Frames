# Website

The Data-Frames marketing site — `index.html`, a single self-contained HTML/CSS/JS file (dark navy/cyan theme, live-scrolling signal log in the hero, services grid, 5-step engagement process, credentials section, pricing section, contact form).

Status: first pass built (2026-08-18). Still needed before this can go live:
- Contact form has no backend — submitting just shows a client-side "thanks" message, nothing is actually sent anywhere yet. Needs wiring to a real endpoint (e.g. an email service or Azure Function) before launch.
- Credentials section leads with "30+ years in IT & Security" and lists CISSP as "in progress," per Aaron's decision not to imply certification before it's official — revisit once he passes.

